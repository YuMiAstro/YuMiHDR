# Normalise text files to LF in the repo; check out as platform-native.
* text=auto eol=lf

# Treat zips and other binaries explicitly.
*.zip binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary

# Windows-only files keep CRLF.
*.ps1 text eol=crlf
